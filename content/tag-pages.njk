---js
// <script>
const pagination = {
	data: "collections",
	size: 1,
	alias: "tag",
	filter: ["all", "posts"],
	// addAllPagesToCollections: true,
};

const eleventyExcludeFromCollections = true;

const eleventyComputed = {
	title: "Tagged '{{ tag }}'",
	permalink: function(data) {
		return `/tags/${this.slugify(data.tag)}/`;
	}
};
---

<div class="mx-auto w-full max-w-4xl p-4">
  <h1
    class="bg-rainbow mb-12 w-fit bg-clip-text text-5xl font-black tracking-tighter text-transparent"
  >
    Tagged
    <em> {{ tag }} </em>
  </h1>

  {% set postslist = collections[ tag ] %}
  {% include "postslist.njk" %}

  <a
    class="hover:after:bg-rainbow hover:bg-rainbow relative bg-clip-text text-2xl font-bold text-blue-500 after:absolute after:-bottom-2 after:left-0 after:h-1 after:w-full after:scale-x-0 after:bg-blue-500 after:transition hover:text-transparent hover:after:scale-x-100 dark:text-blue-400"
    href="blog.njk"
    >See all tags →</a
  >
</div>
