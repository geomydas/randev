---js
// <script>
const pagination = {
	data: "collections",
	size: 1,
	alias: "tag",
	filter: ["all", "posts"],
	// addAllPagesToCollections: true,
};

const eleventyExcludeFromCollections = true;

const eleventyComputed = {
	title: "Tagged '{{ tag }}'",
	permalink: function(data) {
		return `/tags/${this.slugify(data.tag)}/`;
	}
};
---

<div class="mx-auto w-full max-w-4xl p-4">
  <h1
    class="bg-rainbow mb-12 w-fit bg-clip-text text-5xl font-black tracking-tighter text-transparent"
  >
    Tagged
    <em> {{ tag }} </em>
  </h1>

  {% set postslist = collections[ tag ] %}
  {% include "postslist.njk" %}


  <a
    class="font-bold text-2xl text-blue-500 hover:after:bg-rainbow after:scale-x-0 hover:after:scale-x-100 after:transition bg-clip-text hover:bg-rainbow hover:text-transparent dark:text-blue-400 after:w-full relative after:left-0 after:-bottom-2 after:h-1 after:bg-blue-500 after:absolute  "
    href="blog.njk"
    >See all tags →</a
  >
</div>
